import React, { useEffect } from "react";
import PropTypes from "prop-types";
import { connect } from "react-redux";

const ProfileSpotlight = ({ username }) => {
  return <div>Hi</div>;
};

ProfileSpotlight.propTypes = {};

export default ProfileSpotlight;
